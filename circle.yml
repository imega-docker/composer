machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t imega/composer:$CIRCLE_SHA1 .

test:
  install:
    - docker run -rm -v `pwd`:/data imega/composer:$CIRCLE_SHA1 install
